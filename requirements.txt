pandas>=2.2
numpy>=1.26
pyarrow>=15.0
python-dotenv>=1.0
matplotlib>=3.8
plotly>=5.20
streamlit>=1.32
scikit-learn>=1.4
xgboost>=2.0
shap>=0.45
requests>=2.32

# Live Dexcom Share polling (~5 min latency). Optional — refresh.py
# falls back to synthetic data if creds aren't set or the package
# isn't importable.
pydexcom>=0.4

# tconnectsync — reverse-engineered Tandem t:connect API client.
# Pinned to GitHub because PyPI release cadence lags upstream auth fixes.
# No branch specifier — let pip use whatever the upstream default branch is.
tconnectsync @ git+https://github.com/jwoglom/tconnectsync
