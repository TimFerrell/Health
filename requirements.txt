pandas>=2.2
numpy>=1.26
pyarrow>=15.0
python-dotenv>=1.0
matplotlib>=3.8
plotly>=5.20
xgboost>=2.0
shap>=0.45
requests>=2.32

# tconnectsync — reverse-engineered Tandem t:connect API client.
# Pinned to GitHub because PyPI release cadence lags upstream auth fixes.
tconnectsync @ git+https://github.com/jwoglom/tconnectsync@main
